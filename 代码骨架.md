# TermForge 当前代码骨架说明

> 用途：说明当前本地代码骨架是什么，而不是继续保留旧的 React 版伪代码方案

---

## 当前代码现实

当前仓库已经存在以下结构：

```text
TermForge/
├── src-ui/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Terminal.svelte
│   │   │   └── TerminalTab.svelte
│   │   ├── lib/api.ts
│   │   ├── App.svelte
│   │   └── main.ts
│   └── package.json
└── src-tauri/
    ├── src/
    │   ├── commands/
    │   │   ├── session.rs
    │   │   └── store.rs
    │   ├── core/
    │   │   └── session_manager.rs
    │   ├── models/
    │   ├── lib.rs
    │   └── main.rs
    └── Cargo.toml
```

也就是说，当前骨架是：

- 前端：Svelte 4
- 后端：Rust
- 通信：Tauri 命令 + 事件

不是旧文档里的 `React + TS` 骨架。

---

## 当前最重要的骨架职责

### 前端

- 终端容器
- Tab 管理
- API 调用封装
- 事件监听与输出渲染

### 后端

- 会话生命周期管理
- Fake / SSH 会话输入输出
- 连接存储
- 事件推送

---

## 近期应保持的结构方向

```text
src-ui/
├── components/
│   ├── Terminal.svelte
│   ├── TerminalTab.svelte
│   └── 后续连接中心 / SFTP / Runbook 组件
└── lib/
    └── api.ts

src-tauri/
├── commands/
│   ├── session.rs
│   ├── store.rs
│   └── 后续 sftp / forward / runbook
├── core/
│   └── session_manager.rs
└── models/
```

---

## 结论

这份文档之后的作用不是提供一套脱离现实的“理想代码模板”，而是：

- 让后续开发围绕当前骨架前进
- 避免文档继续误导成 React 方案
- 给后续扩展 SFTP / Runbook / 转发提供结构基线
